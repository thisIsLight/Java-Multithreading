/*
 * Description: Build a server that supports multiple clients connecting and chatting with each other. Each client will run in its own thread, and the server will handle broadcasting messages to all connected clients.
Skills Practiced:
Sockets and networking
Thread management and synchronization
Broadcasting messages in a thread-safe manner
Features:
Use a thread for each client connection.
Use a ConcurrentHashMap to store connected clients.
Implement server-side broadcasting to all connected clients.
Optional Enhancements:
Implement private messaging between clients.
Handle disconnection of clients gracefully.
Add a graphical user interface (GUI) for the client.
 */

public class ChatServer {
    
}
